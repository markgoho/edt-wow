import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  styleUrls: ['./app.component.scss'],
  template: `
    <app-logo></app-logo>
  `
})
export class AppComponent {
  title = 'app';
}
